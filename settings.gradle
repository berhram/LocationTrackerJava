dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Tracker for sleepwalkers"
include ':childapp'
include ':parentapp'
include ':sharedcodelibrary'
